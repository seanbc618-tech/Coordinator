// Placeholder — will be fully implemented in Task 6.
import React from 'react'
import { Box, Text } from 'ink'

export function App({ socketPath: _socketPath, projectId: _projectId }: { socketPath: string; projectId: string }) {
  return (
    <Box flexDirection="column">
      <Text>Coordinator TUI — connecting...</Text>
    </Box>
  )
}
